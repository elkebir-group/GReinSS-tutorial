# Python
__pycache__/
*.pyc

# notebook checkpoints
.ipynb_checkpoints/

# transient training artifacts written into the code submodule at runtime
code/_tutorial_*.pt
code/_tutorial_*.npz
code/example_model.pt
code/graph_model.pt

# scratch
_slidepngs/
_chk/
assets/pretrain.log

# generated marp input (assets/svg/*.svg spliced in by scripts/build_slides.py)
slides.gen.md

# generated deck outputs — rebuilt from slides.md by `just all`; pre-built copies
# are attached to GitHub Releases instead of committed (they're large binaries that
# churn on every edit). Run `just all` locally to regenerate.
slides.html
slides.gen.html
slides.pdf
speaker-notes.html
speaker-notes.md
